# Build artifacts — they are produced inside the image and are not needed in the
# context (otherwise a local target/ of several gigabytes slows COPY down and
# invalidates the cache).
/target

# VCS and local settings
.git
.gitignore

# The maintainer's local working notes — they must not reach the image
AGENTS_INTERNAL.md

# Other development environment
.env
*.log
