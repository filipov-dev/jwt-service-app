# Артефакты компиляции — собираются внутри образа, в контекст не нужны
# (иначе локальный target/ на гигабайты тормозит COPY и инвалидирует кеш).
/target

# VCS и локальные настройки
.git
.gitignore

# Приватные заметки агентов (см. AGENTS.md) — не должны попадать в образ
AGENTS_INTERNAL.md

# Прочее окружение разработки
.env
*.log
