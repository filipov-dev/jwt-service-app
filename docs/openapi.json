{
  "openapi": "3.1.0",
  "info": {
    "title": "jwt-service-app",
    "description": "",
    "license": {
      "name": "Apache-2.0",
      "identifier": "Apache-2.0"
    },
    "version": "1.17.14"
  },
  "paths": {
    "/livez": {
      "get": {
        "tags": [
          "handlers"
        ],
        "summary": "Liveness probe: it confirms that the process is alive.",
        "description": "It always returns `200 OK` with no body. Dependencies are not checked — that\nis what [`readyz`] is for. Intended for the orchestrator's liveness check.",
        "operationId": "livez",
        "responses": {
          "200": {
            "description": "The process is alive"
          }
        }
      }
    },
    "/metrics": {
      "get": {
        "tags": [
          "handlers"
        ],
        "summary": "Serves the metrics in the Prometheus exposition format.",
        "description": "Access level 4: a static bearer token (`AUTH_METRICS_TOKEN`) — one that\nPrometheus (`authorization: {credentials_file}`), Zabbix `agent2` and the\nOTel Collector through which Monium scrapes the metrics can all send\nnatively.\n\nThe token is not a substitute for network isolation: the endpoint still\nshould not be exposed publicly, as metrics reveal the operational picture\n(traffic volume, failure ratios, dependency latencies).\n\nThe route is registered in `main.rs` (not through an attribute macro),\nbecause the endpoint is wrapped in the level 4 auth middleware and published\n**conditionally**: without `AUTH_METRICS_TOKEN` it does not exist at all and\nthe path returns `404`.",
        "operationId": "metrics",
        "responses": {
          "200": {
            "description": "The metrics in the Prometheus text format",
            "content": {
              "text/plain": {}
            }
          }
        }
      }
    },
    "/readyz": {
      "get": {
        "tags": [
          "handlers"
        ],
        "summary": "Readiness probe: whether the pod can serve requests.",
        "description": "It pings the `jti` store and requests the JWKS from `jwks-service-app`\n(`GET /.well-known/jwks.json`). It returns `200 OK` when we can serve and\n`503 Service Unavailable` otherwise. In both cases the body is a\n[`ReadinessResponse`] with a breakdown per dependency.\n\n**The probe asks not \"is the dependency alive\" but \"can we answer\".** The\ndifference concerns only the key service: while a usable JWKS snapshot is in\nmemory, verification works without it (see [`crate::jwk`]), so there is no\nreason to take the pod out of the load balancer — otherwise the stale cache\nwould not help in exactly the outage it was built for: readiness would kill\nthe pods within ten seconds and traffic would never reach the snapshot in\nmemory. That state is honestly reported as `degraded`, and once the snapshot\nstops being usable the pod leaves the load balancer on its own.\n\nThe store gets no such leniency: without it the `jti` cannot be checked, so a\nrevoked token would become valid — that is not degradation but a hole.\n\nGeneric over the `jti` store ([`JtiStore`]): the probe reaches it through\n[`JtiStore::ping`] rather than a concrete backend.",
        "operationId": "readyz",
        "responses": {
          "200": {
            "description": "The service is ready to serve requests (`ok` or `degraded`)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessResponse"
                }
              }
            }
          },
          "503": {
            "description": "Requests cannot be served: Redis is unavailable, or the key service has neither answered nor left a usable snapshot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessResponse"
                }
              }
            }
          }
        }
      }
    },
    "/subjects/{sub}/tokens": {
      "delete": {
        "tags": [
          "handlers"
        ],
        "summary": "Revokes every active token of a subject.",
        "description": "Needed on compromise: the caller cannot kill the tokens one by one through\n`DELETE /tokens/{jti}` — it does not know their `jti` values.\n\n# Responses\n- `200 OK` — a [`RevokeGroupResponse`] with the number of revoked tokens\n  (already expired ones do not count, they are invalid anyway);\n- `500 Internal Server Error` — the store is unavailable. Unlike\n  `DELETE /tokens/{jti}`, the error is **not** swallowed: a silent \"success\"\n  for a failed revocation of compromised tokens is more dangerous than an\n  honest error.\n\nGeneric over the `jti` store ([`JtiStore`]).",
        "operationId": "revoke_subject_tokens",
        "parameters": [
          {
            "name": "sub",
            "in": "path",
            "description": "The subject (the `sub` claim) whose tokens are revoked",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokeGroupResponse"
                }
              }
            }
          },
          "401": {
            "description": "Level 3: the TOTP code is missing or invalid",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "429": {
            "description": "The global cap of the endpoint was exceeded (when enabled)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "The store is unavailable — the revocation was NOT performed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "totp": []
          }
        ]
      }
    },
    "/tokens": {
      "post": {
        "tags": [
          "handlers"
        ],
        "summary": "Issues a new JWT.",
        "description": "The request body is a [`TokenRequest`] with `sub` (the subject), `aud` (the\naudience) and an optional `ttl` (a custom lifetime in seconds). The issuer\n(`iss`) is taken from the `Host` header. On issue a `jti` is generated and\nstored in Redis with a TTL equal to the token lifetime.\n\n# Responses\n- `200 OK` — a [`TokenResponse`] with the signed token;\n- `422 Unprocessable Entity` — invalid input (an empty `aud`, an invalid\n  `TOKEN_EXPIRATION_SECONDS` or a `ttl` outside the allowed bounds, for\n  example);\n- `400 Bad Request` — the `Host` header is missing or invalid;\n- `403 Forbidden` — `Host` is not in `TOKEN_ISSUER_ALLOWLIST` (when it is set);\n- `500 Internal Server Error` — other errors (an unavailable JWKS and so on).\n\nGeneric over the `jti` store ([`JtiStore`]) — see the note about the seam at\nthe top of the module.",
        "operationId": "create_token",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TokenRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponse"
                }
              }
            }
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "401": {
            "description": "Level 3: the TOTP code is missing or invalid",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "403": {
            "description": "`Host` is outside `TOKEN_ISSUER_ALLOWLIST` (when it is set)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "422": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "429": {
            "description": "The global cap of the endpoint was exceeded (when enabled)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "totp": []
          }
        ]
      }
    },
    "/tokens/refresh": {
      "post": {
        "tags": [
          "handlers"
        ],
        "summary": "Exchanges a refresh token for a new access + refresh pair.",
        "description": "The old refresh token stops working after the exchange: a new one from the\nsame family is issued. Presenting an already-used token means a leak — the\nwhole family is then killed, the access tokens issued through it included\n(see [`crate::jwt::JwtManager::refresh`]).\n\nAccess level 3 (TOTP), like `POST /tokens`: an exchange is issuing a token,\nit just rests on a presented refresh token rather than on a request from a\ntrusted backend. The endpoint is called by the same internal client that\nissues tokens; the end application never talks to the service directly.\n\n# Responses\n- `200 OK` — a [`TokenResponse`] with a new `token` and `refresh_token`;\n- `401 Unauthorized` — the token is unknown, expired or already used (the\n  details are not revealed, as with token verification);\n- `403 Forbidden` — `Host` is not in `TOKEN_ISSUER_ALLOWLIST` (when it is set);\n- `400 Bad Request` — the `Host` header is missing or invalid.\n\nGeneric over the `jti` store ([`JtiStore`]).",
        "operationId": "refresh_token",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RefreshRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponse"
                }
              }
            }
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "401": {
            "description": "Level 3: no TOTP code, or the refresh token is unknown or already used",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "429": {
            "description": "The global cap of the endpoint was exceeded (when enabled)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "totp": []
          }
        ]
      }
    },
    "/tokens/verify": {
      "post": {
        "tags": [
          "handlers"
        ],
        "summary": "Verifies a JWT.",
        "description": "The request body is a [`TokenVerifyRequest`] with the `token` itself and the\nexpected `audience`. What is checked: the signature (against the public key\nfrom the JWKS found by `kid`), that `iss` matches the `Host` header (and that\nit is acceptable per the issuer allowlist), that `audience` is in `aud`, the\ntime bounds (`nbf`/`iat`/`exp`) and the presence of the `jti` in Redis (not\nrevoked).\n\n# Responses\n- `200 OK` — the token is valid and its claims are returned in the body;\n- `401 Unauthorized` — any verification failure (deliberately without\n  details), including a `Host` outside `TOKEN_ISSUER_ALLOWLIST`;\n- `400 Bad Request` — the `Host` header is missing or invalid.\n\nGeneric over the `jti` store ([`JtiStore`]).",
        "operationId": "verify_token",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TokenVerifyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": ""
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "401": {
            "description": "Level 2: no proxy secret, or the token is invalid or expired",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "429": {
            "description": "The per-IP request limit was exceeded",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "proxy_secret": []
          }
        ]
      }
    },
    "/tokens/{jti}": {
      "delete": {
        "tags": [
          "handlers"
        ],
        "summary": "Revokes a token by its `jti` identifier.",
        "description": "It deletes the `jti` record from Redis; after that, verifying the\ncorresponding token in [`verify_token`] fails.\n\n# Responses\n- `204 No Content` — the token was revoked. **Idempotent**: a `jti` that does\n  not exist also gives `204`, because the desired state has been reached —\n  there is no such token;\n- `500 Internal Server Error` — the store is unavailable and the revocation\n  was **not** performed. Telling this case apart from success is mandatory:\n  the caller is revoking a compromised token and must learn that the attempt\n  failed.\n\nGeneric over the `jti` store ([`JtiStore`]).",
        "operationId": "revoke_token",
        "parameters": [
          {
            "name": "jti",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The token was revoked. Idempotent: a `jti` that does not exist also gives 204"
          },
          "401": {
            "description": "Level 3: the TOTP code is missing or invalid",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "429": {
            "description": "The global cap of the endpoint was exceeded (when enabled)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "The store is unavailable — the revocation was NOT performed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "totp": []
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "ErrorResponse": {
        "type": "object",
        "description": "The unified body of an error response.",
        "required": [
          "error"
        ],
        "properties": {
          "details": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional details."
          },
          "error": {
            "type": "string",
            "description": "A short error message."
          }
        }
      },
      "ReadinessResponse": {
        "type": "object",
        "description": "The response of the readiness check (`GET /readyz`).\n\n`status` is the aggregated state (`\"ok\"`/`\"degraded\"`/`\"unavailable\"`), and\nthe `redis` and `jwks` fields report readiness to serve requests per\ndependency.",
        "required": [
          "status",
          "redis",
          "jwks",
          "jwks_stale"
        ],
        "properties": {
          "jwks": {
            "type": "boolean",
            "description": "Readiness with respect to the key service: it answered **or** memory\nholds a usable JWKS snapshot that verification is served from without it."
          },
          "jwks_stale": {
            "type": "boolean",
            "description": "The key service did not answer and `jwks` rests on a stale snapshot.\nThis is temporary degradation: once the snapshot stops being usable,\n`jwks` turns `false`."
          },
          "redis": {
            "type": "boolean",
            "description": "Redis availability."
          },
          "status": {
            "type": "string",
            "description": "The aggregated status: `\"ok\"` — every dependency is available,\n`\"degraded\"` — we can serve but the key service is not answering itself\n(a snapshot from memory), `\"unavailable\"` — we cannot serve.",
            "example": "ok"
          }
        }
      },
      "RefreshRequest": {
        "type": "object",
        "description": "Body of a refresh token exchange request (`POST /tokens/refresh`).",
        "required": [
          "refresh_token"
        ],
        "properties": {
          "refresh_token": {
            "type": "string",
            "description": "The refresh token received on issue or on a previous exchange."
          }
        }
      },
      "RevokeGroupResponse": {
        "type": "object",
        "description": "The result of a bulk revocation of a subject's tokens.",
        "required": [
          "revoked"
        ],
        "properties": {
          "revoked": {
            "type": "integer",
            "format": "int64",
            "description": "How many active tokens were revoked.\n\nAlready expired ones do not count: they are invalid anyway and there is\nnothing to revoke.",
            "example": 3,
            "minimum": 0
          }
        }
      },
      "TokenRequest": {
        "type": "object",
        "description": "Body of a token issue request (`POST /tokens`).",
        "required": [
          "sub",
          "aud"
        ],
        "properties": {
          "aud": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "example": [
              "api1",
              "api2"
            ]
          },
          "claims": {
            "type": "object",
            "description": "Arbitrary claims that end up in the token payload alongside the\nregistered ones (roles, scope, tenant, an internal id).\n\nThe reserved names (`iss`, `sub`, `aud`, `exp`, `iat`, `nbf`, `jti`)\n**cannot** be overridden — an attempt gives `422`. Otherwise a client\ncould substitute `exp` and bypass the `TOKEN_TTL_MIN_SECONDS` /\n`TOKEN_TTL_MAX_SECONDS` bounds.\n\nThe limits on the number of keys and the total size come from\n`TOKEN_CLAIMS_MAX_COUNT` and `TOKEN_CLAIMS_MAX_BYTES`: a token travels in\nheaders, and a bloated payload breaks proxies.",
            "additionalProperties": {},
            "propertyNames": {
              "type": "string"
            },
            "example": {
              "role": "admin",
              "scope": [
                "read",
                "write"
              ]
            }
          },
          "refresh": {
            "type": "boolean",
            "description": "Issue a refresh token alongside the access token to renew the session.\n\n`false` by default — the contract of existing clients does not change and\na refresh token only appears for those who deliberately asked for one.",
            "example": false
          },
          "sub": {
            "type": "string",
            "example": "user123"
          },
          "ttl": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Optional custom token lifetime in seconds.\n\nWhen unset, `TOKEN_EXPIRATION_SECONDS` is used. When present, the value\nis checked against the `TOKEN_TTL_MIN_SECONDS` /\n`TOKEN_TTL_MAX_SECONDS` bounds; going outside them gives\n`422 Unprocessable Entity`.",
            "example": 3600,
            "minimum": 0
          }
        }
      },
      "TokenResponse": {
        "type": "object",
        "description": "A successful response to a token issue request.",
        "required": [
          "token"
        ],
        "properties": {
          "refresh_token": {
            "type": [
              "string",
              "null"
            ],
            "description": "The refresh token, when one was requested.\n\nAn opaque string rather than a JWT: the client has no reason to parse it,\nit is only presented to `POST /tokens/refresh`. Absent from the response\nwhen no refresh token was requested."
          },
          "token": {
            "type": "string",
            "description": "The signed JWT.",
            "example": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9..."
          }
        }
      },
      "TokenVerifyRequest": {
        "type": "object",
        "description": "Body of a token verification request (`POST /tokens/verify`).",
        "required": [
          "token",
          "audience"
        ],
        "properties": {
          "audience": {
            "type": "string",
            "description": "The expected recipient; must be present in the token's `aud` claim.",
            "example": "api1"
          },
          "token": {
            "type": "string",
            "description": "The whole token being verified (`header.payload.signature`).",
            "example": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9..."
          }
        }
      }
    },
    "securitySchemes": {
      "metrics_token": {
        "type": "http",
        "scheme": "bearer",
        "description": "Level 4: a static bearer token for scraping /metrics (AUTH_METRICS_TOKEN)."
      },
      "proxy_secret": {
        "type": "apiKey",
        "in": "header",
        "name": "X-Proxy-Secret",
        "description": "Level 2: a static secret injected by the reverse proxy. The proxy MUST strip the client-supplied version of the header."
      },
      "totp": {
        "type": "apiKey",
        "in": "header",
        "name": "X-TOTP-Code",
        "description": "Level 3: the current TOTP code (RFC 6238) over the shared secret."
      }
    }
  }
}
