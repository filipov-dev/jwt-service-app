# Envoy — a secret through inline env in the config is not supported directly;
# use substitution when generating the config, or Lua/ext_proc to read it from a
# secret store. Below: request_headers_to_add (an overwrite) plus remove.

static_resources:
  listeners:
  - address: { socket_address: { address: 0.0.0.0, port_value: 443 } }
    filter_chains:
    - filters:
      - name: envoy.filters.network.http_connection_manager
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
          stat_prefix: ingress
          route_config:
            virtual_hosts:
            - name: api
              domains: ["api.example.com"]
              routes:
              - match: { prefix: "/" }
                route: { cluster: jwt_service }
                request_headers_to_add:
                - header: { key: "X-Proxy-Secret", value: "${PROXY_SECRET}" }
                  # append_action REPLACE overwrites the client-supplied version.
                  append_action: OVERWRITE_IF_EXISTS_OR_ADD
                # (1) Additionally delete the incoming header before routing.
                request_headers_to_remove: ["X-Proxy-Secret"]
          http_filters:
          - name: envoy.filters.http.router
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
  clusters:
  - name: jwt_service
    load_assignment:
      cluster_name: jwt_service
      endpoints:
      - lb_endpoints:
        - endpoint: { address: { socket_address: { address: jwt_service, port_value: 8080 } } }
