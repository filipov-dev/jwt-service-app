/target
/.idea

# Внутренние заметки для агентов; каноничная копия — на Вики
# (homepage/projects/jwt-service-app/agents-internal)
AGENTS_INTERNAL.md

# Артефакты нагрузочного прогона: выпущенные токены и выгрузка k6.
# Токены — настоящие подписанные JWT, им в репозитории не место.
/deployments/load/tokens.json
/deployments/load/summary.json

# Боевое окружение: заполненные секреты уровней доступа. В репозитории лежат
# только образцы — .env.example и k8s/secret.example.yaml.
/deployments/prod/.env
/deployments/prod/k8s/secret.yaml

# Мусор macOS Finder: заводится в любой открытой папке, к репозиторию отношения
# не имеет. Без слеша в начале — на любой глубине, а не только в корне.
.DS_Store